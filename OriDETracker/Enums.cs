namespace OriDETracker
{    
    public enum TrackerPixelSizes
    {
        Small = 312,
        Medium = 437,
        Large = 667,
        XL = 750
    }
    public enum AutoUpdateRefreshRates
    {
        rate500mHz = 500,
        rate1Hz = 1000,
        rate10Hz = 10000,
        rate60Hz = 60000
    }
}
